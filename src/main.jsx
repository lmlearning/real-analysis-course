import React from 'react'
import ReactDOM from 'react-dom/client'
import RealAnalysis from '../RealAnalysis.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RealAnalysis />
  </React.StrictMode>,
)
